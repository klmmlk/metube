@@
 [project]
 name = "metube"
 version = "0.1.0"
@@
 dependencies = [
     "aiohttp",
     "python-socketio>=5.0,<6.0",
-    "yt-dlp[default,curl-cffi,deno]",
+    "yt-dlp[default,curl-cffi,deno]",
     "mutagen",
     "curl-cffi",
-    "watchfiles",
+    "watchfiles",
+    "requests",
+    "beautifulsoup4",
 ]
@@
 